# Template — _reversa_sdd/questions.md

This file is generated by the Reviewer and filled in by the user.

---

## How the Reviewer creates this file

For each 🔴 gap that only the user can resolve, create an entry:

```markdown
## Question [N]

**Context:** [Where in the system this doubt arose — module, function, line]
**Affected spec:** [`_reversa_sdd/<unit>/{requirements|design|tasks}.md`]
**Question:** [Direct question, without ambiguity]
**Impact:** [What changes in the spec depending on the answer]

**Answer:** <!-- fill in here -->
```

---

## How the user answers

The user should:
1. Fill in the `**Answer:**` field for each question
2. Notify the Reviewer when done (type `reversa` or "I answered the questions")

---

## How the Reviewer processes the answers

After receiving the user's notification:
1. Read the complete file
2. For each question with a filled-in answer:
   - Update the corresponding spec
   - Reclassify the item according to the answer (🔴→🟢 or 🔴→🟡)
   - Mark the question as `✅ Answered` above the Answer field
3. For unanswered questions: keep as 🔴 in the final report
4. Update `confidence-report.md` with the new percentages

---

## Example of generated file

```markdown
# Questions for Validation — [Project Name]

> Generated by the Reviewer on [date]
> Answer each question and let me know when you are done.

---

## Question 1

**Context:** Module `auth` — function `validateSession()` in `src/auth/session.ts:47`
**Affected spec:** [`_reversa_sdd/auth/requirements.md`]
**Question:** Does the session expire due to inactivity or only by absolute time? The code uses `lastActivity` but there is no visible expiration logic.
**Impact:** If by inactivity, the spec needs to include the maximum idle time and the behavior when renewing the session.

**Answer:** <!-- fill in here -->

---

## Question 2

**Context:** Module `orders` — constant `MAX_ITEMS_PER_ORDER = 50` in `src/orders/constants.ts:12`
**Affected spec:** [`_reversa_sdd/orders/requirements.md`]
**Question:** Is the 50-item limit per order a business rule or a temporary technical limit? Are there plans to increase it?
**Impact:** If it is a business rule, it should appear as 🟢 in the constraints. If it is technical, it should be 🟡 with a future-review note.

**Answer:** <!-- fill in here -->
```
